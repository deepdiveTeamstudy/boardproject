spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PW}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# 원래는 무조건 환경변수 처리해야 합니다!
jwt:
  secret: Kimdoohyeon/Kimbogeun/Sinjimin/Leechanwon/Fighting/EscapeDeadbeat